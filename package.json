{
  "name": "paper-round",
  "version": "0.1.0",
  "private": true,
  "description": "Paper Round — a one-street-at-a-time morning delivery run, drawn on a 2D canvas.",
  "type": "module",
  "scripts": {
    "start": "npx --yes serve . -l 5173",
    "build": "node build.js",
    "test": "node build.js && node test/smoke.test.js && node test/bot.test.js && node test/bundle.test.js"
  }
}
